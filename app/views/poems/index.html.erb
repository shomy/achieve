<% if user_signed_in? %>
  <% @poems.each do |poem| %>
      <p><%= poem.title %></p>
      <p><%= poem.content %></p>
      <p><%= link_to "詳細", poem_path(poem.id) %></p>
  <% end %>

<% else %>
   <h1>ログインして下さい</h1>
<% end %> 